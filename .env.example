# Required
BOT_TOKEN=

DISCORD_DEV_GUILD_ID=

LOG_LEVEL=debug
DATABASE_PATH=./db/data/data.sqlite
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Optional: Ollama server for AI mention replies. Empty = feature disabled.
# Prod sets this via docker-compose (host.docker.internal). For local dev,
# install Ollama and use http://127.0.0.1:11434
OLLAMA_URL=
